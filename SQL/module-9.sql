-- COMPARISON OPERATORS: =, >=, <, <=, =, !=, <>

-- We can use comparison operators to create multiple criteria along with WHERE to filter data

SELECT * FROM users WHERE age = 27;
SELECT * FROM users WHERE age > 20;

